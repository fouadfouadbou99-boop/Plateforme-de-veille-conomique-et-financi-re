version: '3.9'

services:

  economic-intelligence:

    build: .

    container_name: economic-intelligence

    ports:
      - "8501:8501"

    volumes:
      - .:/app

    restart: unless-stopped

    env_file:
      - .env
